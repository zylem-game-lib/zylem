{
  "name": "@zylem/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Rust runtime scaffolding for Zylem simulation and future wasm builds.",
  "license": "MIT",
  "scripts": {
    "build": "cargo build --manifest-path Cargo.toml --target-dir target && cargo build --manifest-path Cargo.toml --target-dir target --target wasm32-unknown-unknown --release",
    "build:production": "cargo build --manifest-path Cargo.toml --target-dir target --target wasm32-unknown-unknown --release",
    "build:native": "cargo build --manifest-path Cargo.toml --target-dir target",
    "build:release": "cargo build --manifest-path Cargo.toml --target-dir target --release",
    "build:wasm": "cargo build --manifest-path Cargo.toml --target-dir target --target wasm32-unknown-unknown --release",
    "test": "cargo test --manifest-path Cargo.toml --target-dir target",
    "typecheck": "cargo check --manifest-path Cargo.toml --target-dir target"
  }
}
